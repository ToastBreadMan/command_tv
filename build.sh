g++ main.cpp -lncurses -o output.out `pkg-config --cflags --libs --static opencv4`
# has to be opencv version 4 so since opencv4 type opencv4 not opencv
# have to use gnome legacy shell (snadart shell)
